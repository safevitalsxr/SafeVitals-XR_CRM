apiVersion: v1
kind: Service
metadata:
  name: safevitals-backend-svc
  namespace: safevitals
  labels:
    app.kubernetes.io/name: safevitals-backend
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: safevitals-backend
